class b  # comment
{
    def main() : int {
        return 10;
    }
}

class a extends b{
    var x : int;
    def testing(a1: int, a2: boolean) : string {
      #s = s.length;
        if(!x == 1 ||
            x[x*2 + 5*x] == true) then {
            writeln(hello.main((2 * (((3))) + x)));
        }

        while(a[34 + 4 + 3 + 3 ]) {
            a = a + 1;
        }

        if (i==4) then {
            i = 5;
        }
        
        return 5 + (-005); 
    }  
    
}




